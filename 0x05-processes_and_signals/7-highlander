#!/usr/bin/env bash
# Script that displays "To infinity and beyond" indefinitely and handles SIGTERM
# Set up signal handler for SIGTERM that does nothing (ignores the signal)
trap '' SIGTERM
# Infinite loop
while true; do
    echo "To infinity and beyond"
    sleep 2
done
