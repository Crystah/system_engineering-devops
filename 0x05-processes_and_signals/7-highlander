#!/usr/bin/env bash
# Script that handles SIGTERM signals silently
# Set up signal handler that exits silently (no output)
trap 'exit 0' SIGTERM
# Your main script logic goes here
# For example, if this is a daemon or long-running process:
while true; do
    # Do your work here
    sleep 1
done
